package com.nubian.ai.sandbox.model;

/**
 * Provider-neutral capability categories exposed by a sandbox provider.
 */
public enum SandboxCapabilityType {
    SESSION,
    FILE_SYSTEM,
    TERMINAL,
    BROWSER,
    DISPLAY,
    PORTS,
    ARTIFACTS,
    COMPUTER
}
